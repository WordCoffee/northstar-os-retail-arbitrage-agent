# Phase A (Approved Gate-1) — Completion Report

**Date:** 2026-09-19T18:08Z (timestamp in filename)
**Scope:** Steps 1–5 as instructed. Read/write-limited. **Nothing committed (per instruction — staged locally only).** No live/paid calls.

---

## 1. What changed (by step)

### Step 1 — FIXES_50 #12 ASIN validation — **CLOSED**
`Northstar_backend/agents/golden_goose_finder/amazon_matcher.py`
- Added `ASIN_RE = ^[A-Z0-9]{10}$` (re.I) and `MOCK_ASIN_RE = ^B0[a-f0-9]{8}$` (re.I).
- Added `is_valid_asin()` (10-char alnum AND not mock-pattern) and `reject_invalid_asin(asin, context)` (raises `ValueError`).
- Gate docstring documents placement: **external boundaries only** (provider results entering matching + ASINs bound for provider calls). The module's own offline mock generator intentionally emits the mock shape and is exempt (its ASINs never reach a provider).
- Applied at the live matching boundary in `_live_find_matches`: provider ASINs failing the gate are dropped with a debug log before becoming `AmazonMatch` objects.

`Northstar_backend/agents/golden_goose_finder/amazon_adapters.py`
- Imports the gate from `amazon_matcher` (no import cycle — matcher imports adapters only lazily).
- `parse_chocodata_results`: invalid/mock-pattern ASINs are dropped at the provider-parse boundary (the audit's referenced entry point).
- `make_easy_parser_seller_caller`, `make_bright_data_offers_caller`, `make_generic_seller_caller` (audit lines 98/275/407 families): each **rejects at factory-creation time**, before any request/closure can be built or provider billed.

Tests (prove rejection):
- `tests/test_amazon_matcher.py` — `TestAsinValidation` (10 tests): real-shape accepted, mock-pattern rejected (case-insensitive), too short/long, non-alnum, empty/None/non-str, `reject_invalid_asin` raise + accept.
- `tests/test_amazon_adapters.py` (new, 16 tests): parser drops mock-pattern/malformed/missing and keeps real ASINs; all three caller factories raise `ValueError` on mock-pattern/malformed/empty and accept a real-shaped ASIN.

### Step 2 — `run_live_scan.py` gate flip — **FLAGGED as Phase B (B4)**
- Added the required comment above `os.environ["GOLDEN_GOOSE_LIVE_OPERATOR_APPROVED"] = "1"`: `SECURITY REVIEW — Phase B (B4): this script can flip the §3 live-gate env var in-process; needs to require an explicit operator-passed flag instead.`
- Created `docs/PHASE_B_SECURITY_ITEMS.md` logging B4 plus the related test-hygiene finding (`test_single_match.py` flips the same env var at import time and runs top-level live discovery on import; async test lacks a pytest async plugin). Nothing fixed — flagged only.

### Step 3 — Golden Goose dry-run / manifest / atomic writes scope — **CONFIRMED NEW WORK**
- `docs/PRODUCTION_BLUEPRINT.md` §16 **Golden Goose Seam Spec (B7)** (new section; TOC updated) explicitly confirms:
  - #49 dry-run → NEW Phase B/C work (the `--mock` default is a test mode that still writes reports; not a dry-run).
  - #23 scan manifest → NEW Phase B/C work (no manifest file exists; `meta` carries only pipeline_version + filters).
  - #10 atomic `save_report` → NEW Phase B/C work (pipeline path is atomic; `goose_report.save_report` writes in place).
- No build work performed (per instruction).

### Step 4 — Duplicate FIXES_50 #26 — **FLAGGED ONLY**
- `FIXES_50.md`: both `26.` lines now carry an inline HTML-comment + note that the file numbers two entries as #26, that items 27–50 continue from the second #26, and that renumbering is deferred to a future doc pass. No renumbering performed.

### Step 5 — A1 blueprint fold-in — **DONE**
`docs/PRODUCTION_BLUEPRINT.md` (Version 1.1, footer/date bumped, TOC extended):
- **§16 Golden Goose Seam Spec (B7)** — see Step 3.
- **§17 Model Strategy Addendum** — rewritten at operator direction to the
  **locked D1–D6 OpenRouter-based DeepSeek strategy** (Alpha Build Blueprint):
  D1 dual-model (V4 Flash volume worker + V4.1 seam model), D2
  `deepseek/deepseek-v4-flash-0731` pinned `relace/fp4` (1M-ctx worker, price
  band), D3 `deepseek/deepseek-v4.1-flash` pinned `deepinfra/fp8`, D4 endpoint
  pinning (`provider.only` + `allow_fallbacks:false`), D5 phase batching
  A(Flash)→B(V4.1)→C(Flash)→D(V4.1) with a gate at each switch, D6 tranche
  funding ($10 funded + 4×$50 = ~$210, hard stop at ~90% of funded balance),
  plus the `:free` 0731 policy, operator-side switch protocol with self-ID
  validation, and the local Ollama fleet demoted to the offline/rollback
  baseline (§17.3). First version (local-fleet addendum) was replaced — see
  this report's §5.1 correction note.
- **§18 Early Paid Launch Boundary** — live gates stay closed; billing not implemented (Stripe = Phase 5); tenant isolation + credit ledger are paid-launch preconditions; named 1-credit probe + circuit breaker rule; operator go/no-go recorded in `00_STATE.json`.
- **§19 Proprietary UX / Security / Admin Boundary** — proprietary presentation is built on a stable public contract; design tokens as the seam (additive, behind `data-theme-v2`); security rules (names-only credentials, test isolation stays OPEN, key rotation standing, in-process gate flips forbidden after B4); admin surfaces operator-only, never the foundation fallback; audit gains `tenant_id` in A2.
- **Tier catalog reconciled** — §8.1 now states `subscription_plans.json` does not exist yet, that the live catalog is `auth.py::PLAN_ENTITLEMENTS` (mirrors §8.2 1:1), the secondary index in `master_brain_subscribers.py` + profile manifest, and that **A2** materializes `shared/subscription-plans.json` with `auth.py` as a thin loader. §8.2 gained a note that entitlements are read-time checks only.

---

## 2. Test results (Step-1 regression evidence)

| Run | Result |
|---|---|
| Golden Goose tests (`pytest agents/golden_goose_finder/tests/`) | **389 passed, 0 failed** (was 363 → +26 new) |
| Full suite, sanctioned composition (`pytest -q --ignore=agents/golden_goose_finder/test_single_match.py`) | **2044 passed, 2 failed, 3 skipped, 48 subtests passed** |
| py_compile (all 4 edited python modules) | Clean |

The 2 residual failures are **pre-existing protected-hash drift**, unrelated to this change:
- `test_proof_batch_run.py` ×2 — `fixtures/protected_hashes.json` baseline hash for `data/scanner-search-cache.json` is stale (`366c…` expected vs `7c0b…` on the committed, git-clean file). Same "legitimate data drift" class recorded in Batch 03 history. My change does not touch or write that file (verified: no git diff for it). Not modified per discipline (protected fixture).

### Baseline-vs-run reconciliation (why 5 failures in the raw full run, 2 in the sanitized one)
Running `pytest` with the whole package tree collected (`test_single_match.py` included) exposes **pre-existing test-isolation defects**, not regressions:
1. `agents/golden_goose_finder/test_single_match.py` sets `GOLDEN_GOOSE_LIVE_OPERATOR_APPROVED=1` at **module import** (line 8) and runs top-level live-discovery code. Importing it leaks the gate open process-wide → the two golden-goose gate tests then fail and live-path tests execute (wrote scratch reports during the run). Confirmed by import probe: env var `'1'` after import. Flagged in `docs/PHASE_B_SECURITY_ITEMS.md` (B4) for Phase B.
2. The same file's `async def test_match` fails with "async def functions are not natively supported" (no async pytest plugin) — environmental.
3. The two `test_proof_batch_run` failures above are drift, present in both runs.
- The sanctioned GG suite composition (module = `tests/` subdir per its documented "363 passed" baseline) is green, and the operator's full-suite baseline (1556 passed/3 skipped) reflects an earlier collection point; my change adds 26 passing tests and regresses nothing.

---

## 3. Working-tree state (staged locally only — nothing committed)

```
 M FIXES_50.md
 M Northstar_backend/agents/golden_goose_finder/amazon_adapters.py
 M Northstar_backend/agents/golden_goose_finder/amazon_matcher.py
 M Northstar_backend/agents/golden_goose_finder/run_live_scan.py
 M Northstar_backend/agents/golden_goose_finder/tests/test_amazon_matcher.py
 M docs/PRODUCTION_BLUEPRINT.md
?? Northstar_backend/agents/golden_goose_finder/tests/test_amazon_adapters.py   (new)
?? docs/PHASE_B_SECURITY_ITEMS.md                                                (new)
```
Pre-existing untracked (present before this session, untouched): `ops/ops_handoff_2026-09-18.md`, `reports/gate0_audit_20260919T173829.md`.

Diff sizes: `amazon_matcher.py` +60 lines · `amazon_adapters.py` +20 · `test_amazon_matcher.py` +43 · `test_amazon_adapters.py` (new, 101) · `run_live_scan.py` +2 (comment) · `FIXES_50.md` 2 lines annotated · `PRODUCTION_BLUEPRINT.md` ≈ +230 lines (4 sections, TOC, footer) · `PHASE_B_SECURITY_ITEMS.md` (new, ~30).

### Diff detail (code only)
- `amazon_matcher.py`: gate constants + 2 functions after the seller-identity block; provider-ASIN guard in `_live_find_matches`.
- `amazon_adapters.py`: import; `parse_chocodata_results` invalid-ASIN skip; `reject_invalid_asin` calls at the top of all three seller caller factories.
- `run_live_scan.py`: 2-line Phase-B comment only.

### Suite-hygiene side effects (handled)
Every full/targeted suite run in this repo writes scratch artifacts (goose_scan reports, `reports/golden_goose/<ts>`, tmp-probe, `_test_quota_tmp.json`, discovery/failure manifests) — the committed history contains the same classes from prior runs. All artifacts my runs created were removed and the three incidentally-dirtied tracked files were restored to HEAD, leaving the tree holding exactly the change set above. No protected fixtures modified.

---

## 4. Ready for approval

Stopping here per instruction. **Not started: subscription catalog migration
(A2) and anything beyond A1.** Awaiting your approval to proceed.

---

## 5. Post-review correction — §17 Model Strategy Addendum (operator 2026-09-19)

The first §17 draft described the **local Ollama fleet** (qwen3/P40/gpt-oss).
The operator rejected it: the alpha engine is the **DeepSeek V4 Flash / V4.1
OpenRouter strategy locked in decisions D1–D6** (Alpha Build Blueprint,
`~/.opencode/plan/northstar_alpha_build_blueprint.md`). §17 was replaced
accordingly:

- §17 header now states the locked strategy and that the local fleet is the
  offline/rollback baseline only.
- §17.1 reproduces D1–D6 (dual-model build, volume-worker pinning/providers/
  price bands, seam-model pinning, endpoint pinning with `allow_fallbacks:false`,
  phase batching with three approval-gated switches, tranche funding $10+4×$50
  ≈ $210 with hard stop at ~90% of funded balance).
- §17.2 covers the `:free` 0731 policy, the free-tier rate limits and
  opportunistic-not-load-bearing rule, and the operator-side model-change
  protocol (stop → prompt → self-ID validation → continue) with the switch map.
- §17.3 demotes the local qwen3 fleet + params to rollback baseline and notes
  the 2026-09-14 trim; §17.4 keeps the bench baseline as engine-hygiene gate.
- Footer bumped to record the correction.

No other §17 content was retained; steps 1, 2, 4, 5 (minus the old §17
summary) and the test results stand as approved. Nothing committed; A2 not
started.

---

Stopping here per instruction. **Not started: subscription catalog migration (A2) and anything beyond A1.** Awaiting your approval to proceed.