# B5 (Cloudflare Deploy Alignment) + B6 (Disclaimer Embed) — Completion Report

**Date:** 2026-09-19T22:50Z. **Nothing committed** (HEAD `6943bda`). No live calls, no spend. Offline alignment/documentation + additive UI text only.

---

## 1. B5 — Cloudflare deploy alignment with bff/v1

### What changed

| File | Change |
|---|---|
| `functions/api/_envelope.js` (new) | Shared bff/v1 envelope helper for Pages Functions: `ok/empty/err`/`internalError`/`notImplemented`, `request_id` (opaque `req_` + 32 hex), HTTP/status mapping, generic (leak-free) messages. Underscore-prefixed → not a route. |
| `functions/api/files.js` | Now returns the bff/v1 envelope: `ok` with `data.files` (KV `scored:index` parsed), `empty` when KV missing/malformed (200, no fabricated rows), `internal_error` on failure (no raw exception). |
| `functions/api/latest-deals.js` | Wraps `loadLatestDeals()` in the envelope (`ok` with `data.rows/meta`); exceptions → generic `internal_error` (never leaks KV/provider text). Relative import corrected to `../../src/lib/dealsApi.js`. |
| `functions/api/data/[filename].js` | Envelope-wrapped: `ok` (+name echo), `not_found` (404) on missing KV key, `invalid_request` (400) on non-`.json` names, generic `internal_error` on failure. |
| `functions/api/transcribe.js` | **Honest stub** — removed the fake `200 {text:""}` success. Returns **501 `not_implemented`** for POST; `invalid_request` (405-style) for other methods, per the bff/v1 taxonomy. |
| `wrangler.toml` | Header comment only — documents Pages + Pages Functions (no Workers compute layer; do not add without sign-off). Zero functional change. |
| `_routes.json` | Verified correct as-is (`/*` static, `/api/*` → Functions) — kept unchanged. |
| `CLOUDFLARE_DEPLOY.md` | Rewritten honestly: real Pages + Functions architecture, KV backing, the bff/v1 envelope, the honest `not_implemented` transcribe stub, troubleshooting entries, and an explicit "no Workers compute layer / local FastAPI is NOT part of this deploy" statement. |
| `docs/contracts/BFF_CONTRACT_v1.md` | Additive taxonomy entry: `not_implemented` → HTTP 501 (used by `transcribe.js`). |

No Workers compute layer, no second deploy target, no new backend — per instruction.

### B5 test (new suite)
`Northstar_backend/test_functions_contract.cjs` — imports each Functions route (real ESM modules, node) with fake contexts (no network; KV stubbed; `csv-parse` real but never reached). Asserts envelope shape, honest statuses/codes, no-leak (provider/exception/path/secret tokens), empty semantics, and the 501 transcribe stub. **25 PASS / 0 FAIL.** Wired into the A5 runner as suite **[5/5]**.

---

## 2. B6 — Disclaimer embed contract

### What changed
- **`docs/contracts/DISCLAIMER_EMBED_v1.md` (new)** — slot registry, placement rules, required copy, FTC-alignment notes, conformance checklist. Cross-references `docs/legal/TERMS_OF_SERVICE.md` and the B2 GG callout as a named, never-remove slot.
- **Additive UI text wired (no slot removed or altered):**

| Slot (`data-slot`) | Surface | Added? |
|---|---|---|
| `landing-footer` | root `index.html` `.footer-bottom` | ✅ |
| `auth-login` | `static/auth/login.html` | ✅ |
| `auth-register` | `static/auth/register.html` | ✅ |
| `spa-footer` | `static/index.html` `#nsFooterDisclaimer` | ✅ |
| `hub-footer` | `static/northstar-os/index.html` | ✅ |
| `hub-onboarding` | hub onboarding modal | ✅ |
| `gg-workspace` | SPA GG seam callout/legend (B2) | existing — untouched ✅ |
| `scout-results` | SPA `table-note`/`calc-callout`/`result-note` | existing — untouched ✅ |
| `gg-export` | GG export manifests (`DEFAULT_DISCLAIMER`) | existing — untouched ✅ |

Required copy per slot is the canonical standard block; the per-service/export wording matches the seams already in use. Nothing removed or weakened.

### B6 test (additive)
`test_ui_display.cjs` **+2** asserts: `#nsFooterDisclaimer` slot present with `data-slot="spa-footer"`, and the required-copy fragments present with no "unlimited" string in the disclaimer block. UI suite **1081 PASS / 0 FAIL**.

---

## 3. Test results — full A5 harness (5 suites)

```
== [1/4] Backend sanitized full suite ==  2102 passed, 2 known pre-existing failures, 0 unexpected | skipped 3 | subtests 48 | OK
== [2/4] Golden Goose module suite ==      431 passed, 0 failed | OK
== [3/4] SPA UI contract suite ==          1081 passed, 0 failed | OK
== [4/4] Shell contract suite ==           251 passed, 0 failed | OK
== [5/5] Cloudflare Functions contract ==  25 passed, 0 failed | OK
AGGREGATE: 2102 backend passed, 2 known pre-existing failures, 0 unexpected -> GREEN
```

`tests/BASELINE.json` updated **only** for legitimate new tests: UI 1079 → **1081** (B6 +2), new `functions` suite **25** (B5). Backend/GG/shell unchanged — nothing masked.

## 4. B1–B7 frozen/complete — with one explicit flag

**Frozen and ready for Phase C:**
- **B1** — `docs/contracts/BFF_CONTRACT_v1.md` (`bff/v1`) ✅
- **B5** — Functions aligned to the envelope; deploy path documented honestly ✅
- **B6** — `docs/contracts/DISCLAIMER_EMBED_v1.md` + slots wired ✅
- **B7** — `docs/contracts/GOLDEN_GOOSE_SEAM_v1.md` + job model / entitlement mapping / export-manifest modules (tested) ✅
- Prior-session lettered work (two-gate live-scan, seam, endpoint pinning, regression) ✅

**FLAG (not silent):** the **Alpha Build Blueprint's Phase B also itemizes B2 (auth/org/tenant/RBAC), B3 (credit ledger), B4 (security boundaries)** — these are **not implemented** anywhere in this repo and, per your earlier direction, are slated to run **under a different model afterward** (your prior: "Do not proceed to B3/B4 — those will run under a different model"). So:

- The **contracts/specs Phase C builds against (B1–B7) are all frozen** — a Phase B final commit of the in-tree B1/B5/B6/B7 + prior work is ready.
- **STOP 2 approval** should record that the Alpha-Plan **implementation** items B2 (auth/tenant/RBAC), B3 (credit ledger), B4 (security boundaries) remain scheduled for the subsequent model-run session, not silently treated as shipped.

## 5. Files (this turn)
- New: `functions/api/_envelope.js`, `Northstar_backend/test_functions_contract.cjs`, `docs/contracts/DISCLAIMER_EMBED_v1.md`, this report.
- Modified: `functions/api/{files,latest-deals,transcribe}.js`, `functions/api/data/[filename].js`, `wrangler.toml`, `CLOUDFLARE_DEPLOY.md`, `docs/contracts/BFF_CONTRACT_v1.md`, `index.html`, `static/auth/{login,register}.html`, `static/index.html`, `static/northstar-os/index.html`, `test_ui_display.cjs`, `scripts/run_all_tests.ps1`, `tests/BASELINE.json`.
- Nothing committed (HEAD `6943bda`). No live calls, no spend.