# A5 — Test Harness Hardening — Completion Report

**Date:** 2026-09-19T19:37Z (timestamp in filename)
**Scope:** A5 per approval. Read/write-limited. **Nothing committed.** No CI/CD added — local runner only. Backend target: 2055/2f/3s/48 · UI 1068/0 · shell 251/0 · GG 389/0

---

## 1. What changed

| File | Purpose |
|---|---|
| `scripts/run_all_tests.ps1` | **Single documented local runner** (PowerShell; native on this Windows host). Runs the four sanctioned suites in order and prints a per-suite pass/fail summary plus a final aggregate line with exit code 0/1. |
| `scripts/run_all_tests.sh` | Thin bash wrapper that executes the same `.ps1` (works via Git Bash here and powershell-core elsewhere). |
| `tests/BASELINE.json` (new) | Canary baseline: recorded counts per suite + the baked-in known exceptions with reasons. |

### Runner behavior
1. **Backend sanitized suite** — `pytest -q --ignore=agents/golden_goose_finder/test_single_match.py`. Parses the summary + the `FAILED …` list, and classifies each failure as *known* (the two `test_proof_batch_run` protected-hash drift ids in `known_exceptions.known_failures`) or *UNEXPECTED*. The single-match exclusion is explicit in the command (reason recorded in the baseline + `docs/PHASE_B_SECURITY_ITEMS.md` B4).
2. **Golden Goose suite** — `pytest -q agents/golden_goose_finder/tests` (389 pass expected).
3. **UI contract suite** — `node test_ui_display.cjs` (1068 PASS expected; requires the `ALL UI DISPLAY TESTS PASSED` marker).
4. **Shell contract suite** — `node tests/test_shell.cjs` (251 PASS expected; requires `ALL GREEN`).
5. **Canary**: every count is diffed against `tests/BASELINE.json`; any movement off baseline (or any UNKNOWN failure) prints a per-suite diff table and fails the aggregate RED.
6. **Honest green line**, e.g.:
   `AGGREGATE: 2055 backend passed, 2 known pre-existing failures, 0 unexpected -> GREEN`

The 2 known hash-drift failures are **tracked, not fixed**, exactly as instructed.

---

## 2. End-to-end run (README line proven)

```
== [1/4] Backend sanitized full suite (pytest) ==
  2055 passed, 2 known pre-existing failures, 0 unexpected | skipped 3 | subtests 48 | OK
== [2/4] Golden Goose module suite (pytest) ==
  389 passed, 0 failed | OK
== [3/4] SPA UI contract suite (node test_ui_display.cjs) ==
  1068 passed, 0 failed | OK
== [4/4] Shell contract suite (node tests/test_shell.cjs) ==
  251 passed, 0 failed | OK

AGGREGATE: 2055 backend passed, 2 known pre-existing failures, 0 unexpected -> GREEN
```
Exit code 0 — **matches baseline exactly.**

### Regression-canary proof (deliberately broken → flagged → reverted)
1. Temporarily set `tests/BASELINE.json` UI baseline **1068 → 99999**.
2. Runner output: `[3/4] 1068 passed, 0 failed | MISMATCH` + `diff: UI passed 1068 != baseline 99999 …` → `AGGREGATE: … 1 suite mismatch(es) / 0 unparsed -> RED` (exit 1). **Flagged correctly.**
3. Reverted baseline to 1068 and re-ran: GREEN again (exit 0).

---

## 3. Working-tree state (nothing committed)

```
?? scripts/run_all_tests.ps1        (new — canonical runner)
?? scripts/run_all_tests.sh         (new — bash wrapper)
?? tests/BASELINE.json              (new — canary baseline)
```
A1–A4 change set carried forward unchanged in this step. Suite scratch artifacts from the three runner executions were removed and incidentally-dirtied tracked files restored — the tree holds exactly the intended change set.

## 4. Flagged items (no unilateral decisions made)
- **Model lane (per your note):** A5 was executed as pure tooling work — no escalation to V4.1, consistent with the volume-worker lane. (The running model is the harness's picked model; no seam-level reasoning was used here.)
- **DeepSeek September pricing:** acknowledged. Your note says official Flash-tier rates rose materially in Sept 2026 and pinned `relace/fp4` / `deepinfra/fp8` endpoints may not track the official card. I cannot read OpenRouter balances (no credential access), so the **quick balance glance after A5–A7 is flagged as operator-side** — a 30-second check, not something I can action. Your $0.81 T1 run rate suggests headroom, as you noted.

## 5. Out of scope — untouched
No CI/CD config, no fix for the 2 known drift failures, no business-logic/code changes. A6 (legal docs) not started.

Awaiting approval to proceed to A6.