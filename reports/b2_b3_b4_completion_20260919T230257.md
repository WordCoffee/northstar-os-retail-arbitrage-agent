# B2 (Auth/RBAC) + B3 (Credit Ledger) + B4 (Security Boundaries) — Completion Report

**Date:** 2026-09-19T23:02Z. **Nothing committed** (HEAD `6943bda`). No live calls, no spend, no billing/Stripe. Offline contracts + minimal wiring only.

---

## 1. Three contract documents

| Doc | Contract id | Contents |
|---|---|---|
| `docs/contracts/AUTH_RBAC_v1.md` | `auth-rbac/v1` | Tenancy (single-tenant-per-account), session/token claims (`sub`, `email`, `plan`, `role`, `tenant_id`, `type`, `jti`, `iat`, `exp`), role hierarchy (`member<operator<owner`, plus separate-axis `admin`), plan tiers from the catalog, **permission matrix** (role × BFF v1 surface, min-plan column), isolation + IDOR/mass-assignment rules. |
| `docs/contracts/CREDIT_LEDGER_v1.md` | `credit-ledger/v1` | Chargeable unit (1 Compute Credit; Creative Credits reserved), per-tier allotment keyed by catalog plan id, action→cost table, C8 credit-meter read contract (server-side balance only), integrity rules (append-only, idempotency, no-negative fail-closed, reserve/commit/release + expiry), entry schema. |
| `docs/contracts/SECURITY_BOUNDARIES_v1.md` | `security-boundaries/v1` | Public vs authenticated vs plan-gated vs admin boundary table, no-leak enforcement points, **technically-enforced least-privilege admin**, threat model (IDOR / mass-assignment / path traversal / export injection / escalation) + mitigations. |

## 2. Code changes (minimal wiring)

| File | Change |
|---|---|
| `Northstar_backend/auth.py` | Added `ROLE_*`, `VALID_ROLES`, `ROLE_ORDER`, `PLAN_ORDER`, `is_valid_role`, `role_rank`, `plan_rank`, `is_admin`, `has_role`, `tenant_id_of`, `admin_account_view` (fail-closed; admin excluded from tenant-role checks). `create_access_token` gained **additive** `role`/`tenant_id`/`jti` claims (unknown role → `member`; defaults preserve prior behavior). |
| `Northstar_backend/auth_deps.py` | `get_current_user` now surfaces `role`/`tenant_id` (demo fallback = `member`/`anonymous`); added `require_role(min_role)` and `require_admin` (both fail closed). |
| `Northstar_backend/main.py` | Added `GET /api/v1/admin/accounts/{user_id}` guarded by `require_admin`, returning only `auth.admin_account_view` (whitelisted `id/email/plan/created_at`); 404 on unknown id. Additive; no existing route changed. |
| `Northstar_backend/credit_ledger.py` (new) | `CreditLedger` (`grant/reserve/commit/release/consume/balance/entries`), `InsufficientCredits`, `UnknownPlanError`, `PLAN_CREDIT_ALLOTMENT` + `allotment_for_plan` validated against the catalog, idempotency by key, reservation expiry, append-only storage (in-memory; optional JSONL). No billing. |

Functions boundary: verified read-only + envelope-only by the B5 contract test; edge Access documented in `CLOUDFLARE_DEPLOY.md` (no code change needed — the Functions are edge-gated and leak-free).

## 3. New tests (+28)

- `test_auth_rbac.py` (13): role validity/rank, unknown fail-closed, hierarchy, **admin ≠ tenant role**, tenancy, plan rank, admin whitelist excludes `password_hash`/tokens/role, additive token claims, unknown-role→member, defaults.
- `test_credit_ledger.py` (11): grant/balance, consume, **insufficient fail-closed (no negative)**, **idempotency no double-charge**, reserve/commit/release, reserve-insufficient, append-only + balance=sum + copy isolation, allotment validation.
- `test_security_boundaries.py` (4): `require_admin` allow/deny (403), `require_role` fail-closed (admin denied tenant role), admin endpoint 403 for anonymous, admin endpoint 200 with **field whitelist**, unknown id 404, `/api/v1/auth/me` demo path intact.

## 4. Test results — full 5-suite harness

```
== [1/4] Backend sanitized full suite ==  2130 passed, 2 known pre-existing failures, 0 unexpected | skipped 3 | subtests 48 | OK
== [2/4] Golden Goose module suite ==      431 passed, 0 failed | OK
== [3/4] SPA UI contract suite ==          1081 passed, 0 failed | OK
== [4/4] Shell contract suite ==           251 passed, 0 failed | OK
== [5/5] Cloudflare Functions contract ==  25 passed, 0 failed | OK
AGGREGATE: 2130 backend passed, 2 known pre-existing failures, 0 unexpected -> GREEN
```
`tests/BASELINE.json` updated **only** for the legitimate +28 backend tests (2102 → 2130). GG/UI/shell/functions unchanged. The 2 known failures are the pre-existing protected-hash drift.

## 5. B1–B7 ALL frozen and complete — ready for the final Phase B commit + full STOP 2

| Item | Status | Artifact |
|---|---|---|
| **B1** BFF/API contract | ✅ FROZEN | `docs/contracts/BFF_CONTRACT_v1.md` |
| **B2** Auth/tenant/RBAC | ✅ FROZEN + wired | `docs/contracts/AUTH_RBAC_v1.md`, `auth.py`, `auth_deps.py` |
| **B3** Credit ledger | ✅ FROZEN + wired | `docs/contracts/CREDIT_LEDGER_v1.md`, `credit_ledger.py` |
| **B4** Security boundaries | ✅ FROZEN + wired | `docs/contracts/SECURITY_BOUNDARIES_v1.md`, `require_admin` + admin endpoint |
| **B5** Cloudflare deploy alignment | ✅ FROZEN + wired | `functions/api/_envelope.js` + aligned routes, `CLOUDFLARE_DEPLOY.md`, functions suite |
| **B6** Disclaimer embed | ✅ FROZEN + wired | `docs/contracts/DISCLAIMER_EMBED_v1.md` + slots |
| **B7** GG seam spec | ✅ FROZEN + wired | `docs/contracts/GOLDEN_GOOSE_SEAM_v1.md` + job/entitlement/manifest modules |

**All of B1–B7 are now frozen and complete; the full 5-suite harness is GREEN with only the 2 known pre-existing failures.** Phase B is ready for its final commit and full **STOP 2 approval**.

**Flags (no silent decisions):**
1. **Credit allotments** (`PLAN_CREDIT_ALLOTMENT`) and **GG entitlements** are **proposed** mappings keyed by catalog plan ids — operator sign-off recommended before Phase C; both are validated against the catalog and duplicate no catalog data.
2. **Alpha auth is stub/mock** (demo fallback = `member`/`anonymous`); a real login UI/session store and DB-backed role column are Phase C (per instruction). Role/tenant are carried on the token and enforced fail-closed.
3. **Backend endpoints still return plain dicts**, not the bff/v1 envelope — envelope adoption is Phase C wiring (B1 §9 / B7 §6); Functions already conform.
4. No billing/Stripe; no live scanning (`/scan` stays 403).

## 6. Files (this turn)
- New: `docs/contracts/{AUTH_RBAC_v1,CREDIT_LEDGER_v1,SECURITY_BOUNDARIES_v1}.md`, `Northstar_backend/credit_ledger.py`, `Northstar_backend/{test_auth_rbac,test_credit_ledger,test_security_boundaries}.py`, this report.
- Modified: `auth.py`, `auth_deps.py`, `main.py`, `tests/BASELINE.json`.
- Nothing committed (HEAD `6943bda`). No live calls, no spend.