# A3 — Design Tokens + Demo-Data Re-sync — Completion Report

**Date:** 2026-09-19T19:01Z (timestamp in filename)
**Scope:** A3 per approval. Read/write-limited. **Nothing committed** (staged locally only). Golden Goose panel, landing, and auth pages NOT touched.

---

## 1. What changed

### 1.1 `shared/design-tokens.json` — created (canonical token spec)
Single canonical source for the six blueprint token categories: **color, type scale, spacing, radius, elevation, motion**, each with light + dark mode values (`modes: ["dark","light"]`, `default_mode: "dark"`). Version-1 values transcribed **verbatim** from the SPA's current active theme (DESIGN SYSTEM v2 blocks at `static/index.html`) so enabling v2 is pixel-identical to today; token tuning now happens here, never in SPA markup.

### 1.2 `shared/design-tokens.css` — created (canonical CSS)
- **Token layer** (`html[data-theme-v2]` = dark, `html[data-theme-v2][data-theme="light"]` = light) defining `--ns-*` tokens.
- **Alias layer** (`html[data-theme-v2]`) mapping every existing SPA variable name to the tokens — `--bg-app → var(--ns-color-bg-app)`, `--font/--mono`, `--text-xs…xl`, `--sp-1…10`, `--radius/--radius-sm/--card-r`, `--shadow/--shadow-sm/md/lg`, `--dur/--ease`. **No existing variable renamed or removed.** Layout-only vars (`--rail-*`, `--drawer-width`, `--topbar-h`, `--gallery-gap`, `--grad*`) explicitly excluded (not tokens) and documented as such.

### 1.3 `static/index.html` — embedded token CSS (additive, gated)
- Embedded the `design-tokens.css` content **verbatim** between `A3 DESIGN-TOKENS: BEGIN/END` markers, inserted at the very end of the main `<style>` block — so it loses no specificity tie to earlier `:root`/`html[data-theme=…]` blocks.
- **Gating verified:** the default markup `<html lang="en" data-theme="light">` carries **no** `data-theme-v2` attribute → the **old theme remains the default**. The new token+alias layers activate only when a future `data-theme-v2` attribute is added (the plan after the UI contract passes on v2).
- No JS, element IDs, or data attributes were changed; the UI contract suite runs against the v1-default theme.

### 1.4 `static/northstar-os/js/demo-data.js` — re-synced to the canonical catalog (data-shape only)
- `autothink.entitled_gates` now equals the canonical 9-gate array **including `autothink_workspace`, in catalog order** (was missing the gate; order was reversed).
- All four plan `blurb`s replaced with the canonical `description`s (foundation "Demo/read-only access" … autothink "Everything + AutothinK AI workspace").
- Mirror comment updated to cite `shared/subscription-plans.json`.
- **Zero network added**; hub stays zero-network by design (test_shell green). `engine.js`/`account.js`/`NS.gates` untouched.

### 1.5 Test harnesses — additive assertions only (no replacements)
- `test_ui_display.cjs` **+10 A3 asserts**: default `<html>` has no `data-theme-v2` (old theme default), token layer gated, dark + light token values present, alias wiring (`--bg-app`, `--card-r`, `--dur`, `--ease`), and a **sync guard** asserting the embedded SPA block equals `shared/design-tokens.css` verbatim (CRLF-normalized) so the two can never drift.
- `test_shell.cjs` **+6 A3 asserts**: demo autothink gate array equals the canonical JSON exactly (order included), four plans present, no "unlimited" language, autothink blurb matches canonical description, mirror cites the shared catalog path.

---

## 2. Test results

| Run | Result (exit) |
|---|---|
| `node test_ui_display.cjs` (from Northstar_backend) | **ALL UI DISPLAY TESTS PASSED** — 1055 PASS / 0 FAIL (985 pre-existing + 10 new A3; the file's assertion count has grown past the recorded 986 via previously approved work, e.g. 1030 PASS recorded 2026-09-15) |
| `node tests/test_shell.cjs` (from static/northstar-os) | **ALL GREEN** — 251 PASS / 0 FAIL (245 pre-existing + 6 new A3) |
| Backend sanitized full suite (`pytest -q --ignore=...test_single_match.py`) | **2055 passed / 2 failed / 3 skipped / 48 subtests** — identical to the A2 baseline; the 2 failures are the SAME pre-existing `test_proof_batch_run` protected-hash drift (`scanner-search-cache.json`) |

No regression anywhere. A3 touched no backend Python, so the backend numbers are unchanged (as expected).

---

## 3. data-theme-v2 gate — working

- Old theme is the default: the served `<html>` tag is unchanged (`<html lang="en" data-theme="light">`, no v2 attr) — asserted by the harness.
- The new tokens + aliases are inert until `data-theme-v2` is set on `<html>` (planned only after the full UI contract passes on v2, per blueprint tokens spec).
- Enabling v2 today is pixel-identical by construction (v1 token values = current SPA values), so the gate can be flipped without a visual flash when approved.

---

## 4. Working-tree state (nothing committed)

```
 M Northstar_backend/static/index.html
 M Northstar_backend/static/northstar-os/js/demo-data.js
 M Northstar_backend/static/northstar-os/tests/test_shell.cjs
 M Northstar_backend/test_ui_display.cjs
?? shared/design-tokens.css
?? shared/design-tokens.json
```
(A1/A2 files carried forward unchanged; suite scratch artifacts from the backend run were removed and incidentally-dirtied tracked files restored — tree holds exactly the intended change set.)

## 5. Out of scope — untouched
Golden Goose panel styling, landing page, auth pages. A4 (SPA redesign foundation) not started.

Awaiting approval before proceeding to A4 or anything else.